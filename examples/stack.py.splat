class Stack():
  def __init__(self):
    self.items = []

  def push(self, item):
    self.items.append(item)

  def pop(self):
    return self.items.pop()

s = Stack()
s.push(1)
s.push(2)
s.push(3)

print(s.pop())
print(s.pop())
print(s.pop())

%% Unterminated %output tag considers the rest of the
%% file to be the expected output:

%output
3
2
1
